package db.marmot.repository;

import db.marmot.volume.CustomRepository;

/**
 * @author shaokang
 */
public class DataSourceRepository extends CustomRepository {
	
	public DataSourceRepository(DataSourceTemplate dataSourceTemplate) {
		super(dataSourceTemplate);
	}
}
